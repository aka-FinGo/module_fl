name: cabix_konveyer
description: Cabix Konveyer ERP tizimi.
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  mobile_scanner: ^5.0.0
  http: ^1.2.1
  shared_preferences: ^2.2.2
  url_launcher: ^6.3.0
  youtube_player_flutter: ^8.1.2
  pdfx: ^2.5.0 # PDF larni ichkarida ochish uchun
  flutter_windowmanager: ^0.2.0
  audioplayers: ^6.6.0
  webview_flutter: ^4.7.0
  pointer_interceptor: ^0.10.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - web/icons/
